<svg
  class="academia-icon w-24 h-24 md:w-32 md:h-32 text-warm-900 dark:text-dark-900 transition-all duration-300"
  viewBox="0 0 120 120"
  fill="none"
  xmlns="http://www.w3.org/2000/svg"
  aria-hidden="true"
>
  <!-- Simple Open Book -->
  <g class="book-element">
    <!-- Left page -->
    <path
      d="M 25 75 Q 25 45, 30 35 L 55 35 L 60 75 Z"
      stroke="currentColor"
      stroke-width="1.5"
      fill="none"
      class="book-page-left"
    />
    <!-- Right page -->
    <path
      d="M 60 75 L 65 35 L 90 35 Q 95 45, 95 75 Z"
      stroke="currentColor"
      stroke-width="1.5"
      fill="none"
      class="book-page-right"
    />
    <!-- Book spine -->
    <line
      x1="60" y1="35"
      x2="60" y2="75"
      stroke="currentColor"
      stroke-width="2"
      class="book-spine"
    />
  </g>
</svg>

<style>
  .academia-icon {
    transform-origin: center;
  }

  .book-page-left {
    transform-origin: 60px 55px;
    transition: transform 0.4s ease;
  }

  .book-page-right {
    transform-origin: 60px 55px;
    transition: transform 0.4s ease;
  }

  :global(.group:hover) .book-page-left {
    transform: rotateY(-10deg);
  }

  :global(.group:hover) .book-page-right {
    transform: rotateY(10deg);
  }

  @media (prefers-reduced-motion: reduce) {
    .book-page-left,
    .book-page-right {
      transition: none !important;
    }
  }
</style>
