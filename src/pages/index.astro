---
// Root redirect to default language for SEO
export const prerender = true;

// This will redirect the root URL to the default language
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
return Astro.redirect(`${base}/en/research`, 302);
---