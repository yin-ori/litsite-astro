---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { useTranslations } from '@/utils/i18n';
import { SITE_CONFIG } from '@/constants/site';

const locale = 'de';
const t = useTranslations(locale);

const title = t('contact.title');
const description = t('contact.description');
---

<BaseLayout
  title={title}
  description={description}
  locale={locale}
  keywords={['kontakt', 'übersetzung', 'angebot', 'anfrage']}
>
  <div class="min-h-screen bg-warm-50 dark:bg-dark-50">
    <div class="max-w-2xl mx-auto px-6 py-16">
      <!-- Simple header -->
      <div class="mb-16">
        <h1 class="text-2xl font-medium text-warm-900 dark:text-dark-900 mb-6">Kontakt</h1>
        <p class="text-warm-700 dark:text-dark-700 leading-relaxed">
          Möchtest du dein Übersetzungs- oder Lektoratsprojekt besprechen? Nimm Kontakt auf,
          um das Gespräch zu beginnen.
        </p>
      </div>

      <!-- Simple contact form -->
      <div>
        <h2 class="text-lg font-medium text-warm-900 dark:text-dark-900 mb-6">Nachricht senden</h2>

        <form
          class="space-y-6"
          action="https://formspree.io/f/xblgaqbj"
          method="POST"
        >
          <div class="space-y-4">
            <div>
              <input
                type="text"
                name="name"
                placeholder="Dein Name"
                required
                class="w-full px-0 py-3 bg-transparent border-0 border-b border-warm-300 dark:border-dark-300 focus:border-warm-700 dark:focus:border-dark-700 focus:outline-none text-warm-900 dark:text-dark-900 placeholder-warm-500 dark:placeholder-dark-500 transition-colors"
              />
            </div>

            <div>
              <input
                type="email"
                name="email"
                placeholder="Deine E-Mail-Adresse"
                required
                class="w-full px-0 py-3 bg-transparent border-0 border-b border-warm-300 dark:border-dark-300 focus:border-warm-700 dark:focus:border-dark-700 focus:outline-none text-warm-900 dark:text-dark-900 placeholder-warm-500 dark:placeholder-dark-500 transition-colors"
              />
            </div>

            <div>
              <select
                name="service"
                required
                class="w-full px-0 py-3 bg-transparent border-0 border-b border-warm-300 dark:border-dark-300 focus:border-warm-700 dark:focus:border-dark-700 focus:outline-none text-warm-900 dark:text-dark-900 transition-colors"
              >
                <option value="">Art der Dienstleistung wählen</option>
                <option value="translation">Übersetzung</option>
                <option value="proofreading">Korrekturlesen</option>
                <option value="consultation">Beratung</option>
                <option value="other">Sonstiges</option>
              </select>
            </div>

            <div>
              <textarea
                name="message"
                placeholder="Erzähl mir von deinem Projekt"
                rows="5"
                required
                class="w-full px-0 py-3 bg-transparent border-0 border-b border-warm-300 dark:border-dark-300 focus:border-warm-700 dark:focus:border-dark-700 focus:outline-none text-warm-900 dark:text-dark-900 placeholder-warm-500 dark:placeholder-dark-500 resize-none transition-colors"
              ></textarea>
            </div>
          </div>

          <div class="pt-4">
            <button
              type="submit"
              class="text-warm-900 dark:text-dark-900 hover:text-warm-700 dark:hover:text-dark-700 transition-colors underline underline-offset-4 font-medium"
            >
              Nachricht senden
            </button>
          </div>

          <!-- Hidden fields for better form handling -->
          <input type="hidden" name="_language" value="de" />
          <input type="hidden" name="_subject" value="Neue Anfrage von jmsugawara.com" />
        </form>
      </div>

      <!-- Project information -->
      <div class="mt-16 pt-8 border-t border-warm-300 dark:border-dark-300">
        <h2 class="text-lg font-medium text-warm-900 dark:text-dark-900 mb-4">Projektinformationen</h2>
        <div class="space-y-3 text-warm-700 dark:text-dark-700 text-sm">
          <p>Bei der Kontaktaufnahme ist es hilfreich, folgende Informationen anzugeben:</p>
          <ul class="text-warm-600 dark:text-dark-600 space-y-1 ml-4 list-disc">
            <li>Art des Textes (literarisch, akademisch, geschäftlich usw.)</li>
            <li>Ausgangs- und Zielsprache</li>
            <li>Ungefähre Wort- oder Seitenzahl</li>
            <li>Zeitrahmen oder Fristanforderungen</li>
            <li>Besondere Anforderungen oder Wünsche</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>