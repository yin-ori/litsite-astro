---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { useTranslations } from '@/utils/i18n';
import { SITE_CONFIG } from '@/constants/site';

const locale = 'ja';
const t = useTranslations(locale);

const title = t('contact.title');
const description = t('contact.description');
---

<BaseLayout
  title={title}
  description={description}
  locale={locale}
  keywords={['お問い合わせ', '翻訳', '見積もり', 'お問い合わせフォーム']}
>
  <div class="min-h-screen bg-warm-50 dark:bg-dark-50">
    <div class="max-w-2xl mx-auto px-6 py-16">
      <!-- Simple header -->
      <div class="mb-16">
        <h1 class="text-2xl font-medium text-warm-900 dark:text-dark-900 mb-6">お問い合わせ</h1>
        <p class="text-warm-700 dark:text-dark-700 leading-relaxed">
          翻訳や校正プロジェクトについてご相談の準備はできましたか？
          お気軽にお問い合わせください。
        </p>
      </div>

      <!-- Simple contact form -->
      <div>
        <h2 class="text-lg font-medium text-warm-900 dark:text-dark-900 mb-4">メッセージを送信</h2>

        <form
          class="space-y-6"
          action="https://formspree.io/f/xblgaqbj"
          method="POST"
        >
          <div class="space-y-4">
            <div>
              <input
                type="text"
                name="name"
                placeholder="お名前"
                required
                class="w-full px-0 py-3 bg-transparent border-0 border-b border-warm-300 dark:border-dark-300 focus:border-warm-700 dark:focus:border-dark-700 focus:outline-none text-warm-900 dark:text-dark-900 placeholder-warm-500 dark:placeholder-dark-500 transition-colors"
              />
            </div>

            <div>
              <input
                type="email"
                name="email"
                placeholder="メールアドレス"
                required
                class="w-full px-0 py-3 bg-transparent border-0 border-b border-warm-300 dark:border-dark-300 focus:border-warm-700 dark:focus:border-dark-700 focus:outline-none text-warm-900 dark:text-dark-900 placeholder-warm-500 dark:placeholder-dark-500 transition-colors"
              />
            </div>

            <div>
              <select
                name="service"
                required
                class="w-full px-0 py-3 bg-transparent border-0 border-b border-warm-300 focus:border-warm-700 focus:outline-none text-warm-900 transition-colors"
              >
                <option value="">サービスを選択してください</option>
                <option value="translation">翻訳</option>
                <option value="proofreading">校正</option>
                <option value="consultation">コンサルテーション</option>
                <option value="other">その他</option>
              </select>
            </div>

            <div>
              <textarea
                name="message"
                placeholder="プロジェクトについてお聞かせください"
                rows="5"
                required
                class="w-full px-0 py-3 bg-transparent border-0 border-b border-warm-300 focus:border-warm-700 focus:outline-none text-warm-900 placeholder-warm-500 resize-none transition-colors"
              ></textarea>
            </div>
          </div>

          <div class="pt-4">
            <button
              type="submit"
              class="text-warm-900 dark:text-dark-900 hover:text-warm-700 dark:hover:text-dark-700 transition-colors underline underline-offset-4 font-medium"
            >
              メッセージを送信
            </button>
          </div>

          <!-- Hidden fields for better form handling -->
          <input type="hidden" name="_language" value="ja" />
          <input type="hidden" name="_subject" value="jmsugawara.comからの新しいお問い合わせ" />
        </form>
      </div>

      <!-- Project information -->
      <div class="mt-16 pt-8 border-t border-warm-300 dark:border-dark-300">
        <h2 class="text-lg font-medium text-warm-900 dark:text-dark-900 mb-4">プロジェクト情報</h2>
        <div class="space-y-3 text-warm-700 dark:text-dark-700 text-sm">
          <p>お問い合わせの際、以下の情報をお知らせいただけると助かります：</p>
          <ul class="text-warm-600 dark:text-dark-600 space-y-1 ml-4 list-disc">
            <li>テキストの種類（文学、学術、ビジネスなど）</li>
            <li>原文と翻訳先の言語</li>
            <li>おおよその語数またはページ数</li>
            <li>スケジュールや締切要件</li>
            <li>特別な要件やご希望</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>